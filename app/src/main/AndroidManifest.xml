<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.esther">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/car"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MYCOSTOMER"
            android:parentActivityName=".add_custmer"></activity>
        <activity android:name=".customer" />
        <activity android:name=".constomerinput" />
        <activity android:name=".home"
            android:parentActivityName=".MYCOSTOMER"/>
        <activity android:name=".mycustomer" />
        <activity android:name=".add_custmer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>